'use strict';

/**
 * categoria-contenido router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::categoria-contenido.categoria-contenido');
