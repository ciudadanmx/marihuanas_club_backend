'use strict';

/**
 * categoria-contenido service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::categoria-contenido.categoria-contenido');
